[global]
   workgroup = WORKGROUP
   server role = standalone server
   map to guest = Bad User
   logging = file
   log file = /var/log/samba/log.%m
   max log size = 1000

[public]
   path = /srv/samba/public
   browseable = yes
   read only = no
   guest ok = yes
   force user = nobody
